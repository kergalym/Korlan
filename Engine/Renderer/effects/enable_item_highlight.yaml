# Highlight effect

vertex:
    transform: |

fragment:
    inout: |

    includes: |
        #pragma include "Engine/Shaders/particles_func.inc.glsl"

    defines: |
        #define DONT_FETCH_DEFAULT_TEXTURES 0
        #define DONT_SET_MATERIAL_PROPERTIES 1

    material: |
        vec3 colorA = vec3(0.030531512498855596, 0.004492279797792435, 0.000447507366538048);
        m.basecolor = colorA;
        m.shading_model = 1; //mInput.shading_model; // transparency 3
        m.specular_ior = 0.0;
        m.roughness = 0.0;
        m.metallic = mInput.metallic;
        m.shading_model_param0 = mInput.arbitrary0;
        m.normal = vOutput.normal;